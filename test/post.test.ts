import "mocha";
import * as chai from "chai";
chai.should();

import * as PostComponent from "../src/components/post";

describe("Post Actions", function(){
    it("should create an action on post create");
    it("should create an action on post delete");
    it("should create an action on post favorite");
    it("should create an action on post comment");
});
